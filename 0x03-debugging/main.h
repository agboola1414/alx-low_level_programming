#ifndef MAIN_H
#ifndef MAIN_H

#include <stdio.h>

void postitive_or_negative(int i);
int largest_number(int a, int b, int c);
void print_remainig_days(int month, int day, int year);
int conver_days(int month, int day);
void print_times_table(int n);

#endif
